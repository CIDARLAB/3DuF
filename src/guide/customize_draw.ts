var is_customize = 0;
function draw_diy(obj){
	point_path = [];
	var obj = document.getElementById("tb4-1");
	obj.innerHTML = "";
	is_customize = 1;
	num_open = point_path;
}

// chosen ports will close all valves connect to them, and we will simulate again and give the customized result